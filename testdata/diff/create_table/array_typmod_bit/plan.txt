Plan: 1 to modify.

Summary by type:
  tables: 1 to modify

Tables:
  ~ flags
    + fixed_bits (column)
    + var_bits (column)

DDL to be executed:
--------------------------------------------------

ALTER TABLE flags ADD COLUMN fixed_bits bit(8)[];

ALTER TABLE flags ADD COLUMN var_bits varbit(16)[];
